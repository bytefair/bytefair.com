{{>header}}
  <article>
    <header>
      <h1>{{title}}</h1>
      <time datetime="{{date}}">{{moment date format="dddd, MMMM DD, YYYY"}}</time>
    </header>
    {{{contents}}}
  </article>
{{>footer}}
